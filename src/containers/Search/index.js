import React,{Component} from 'react';import './index.less'import Header from "../../component/Header/index";import {connect} from "react-redux"import actions from "../../store/actions/search"class Search extends Component{    handleClick=()=>{        let keyWords=this.keyWords.value;        this.props.fetchSearch(keyWords);    };    render(){        return (            <div>                <Header title="搜索"/>                <div className='search'>                    <i className="iconfont icon-sousuo1"></i>                    <input placeholder='2017全球特色圣诞礼' ref={input=>this.keyWords=input} type="text"/>                    <div className="search-btn" onClick={this.handleClick}>搜索</div>                    {                        this.props.search&&this.props.search.map(item=>(                            <img src={item.recommendImg} alt=""/>                        ))                    }                </div>            </div>        )    }}export default connect(    state=>state.search,    actions)(Search);